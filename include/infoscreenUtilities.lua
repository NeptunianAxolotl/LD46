


local function HandleClick(infoscreenData, world, interface, mouseX, mouseY)

end

local function ButtonClicked(infoscreenData, world, interface, mouseX, mouseY)

end

local function KeyPressed(infoscreenData, world, interface, key)

end

local function DrawButtonHover(infoscreenData, world, interface, mouseX, mouseY)

end

local function Draw(infoscreenData, world, interface, mouseX, mouseY)

end

return {
	HandleClick = HandleClick,
	ButtonClicked = ButtonClicked,
	KeyPressed = KeyPressed,
	DrawButtonHover = DrawButtonHover,
	Draw = Draw,
}
