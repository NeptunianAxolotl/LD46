


local function HandleClick(infoscreenData, world, mouseX, mouseY)

end

local function ButtonClicked(infoscreenData, world, mouseX, mouseY)

end

local function DrawButtonHover(infoscreenData, world, mouseX, mouseY)

end

local function Draw(infoscreenData, world, mouseX, mouseY)

end

return {
	HandleClick = HandleClick,
	ButtonClicked = ButtonClicked,
	DrawButtonHover = DrawButtonHover,
	Draw = Draw,
}
